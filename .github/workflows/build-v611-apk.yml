name: V6.1.1 R4 Verify and APK Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'R4 payload transfer in progress; push build temporarily paused.'
