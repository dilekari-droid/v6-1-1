<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_screen_gradient">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="16dp">
        <TextView android:id="@+id/searchBack" android:layout_width="wrap_content" android:layout_height="40dp" android:background="@drawable/bg_chip" android:gravity="center" android:paddingHorizontal="12dp" android:text="‹ Geri" android:textColor="@color/text_primary" android:textStyle="bold" />
        <TextView style="@style/Title" android:layout_marginTop="12dp" android:text="Ayar Ara" />
        <android.widget.SearchView
            android:id="@+id/settingsSearchView"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_card"
            android:iconifiedByDefault="false"
            android:queryHint="API, tarama, bildirim…" />
        <TextView android:id="@+id/searchEmpty" style="@style/Sub" android:layout_marginTop="10dp" android:visibility="gone" />
        <ListView
            android:id="@+id/settingsSearchResults"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="8dp"
            android:divider="@color/stroke_soft"
            android:dividerHeight="1dp" />
    </LinearLayout>
    <include layout="@layout/include_bottom_nav" />
</LinearLayout>
